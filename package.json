{
  "scripts": {
    "build": "cp node_modules/@mercuryworkshop/wisp-js/src/server/connection.mjs src/connection.mjs && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@mercuryworkshop/wisp-js": "latest"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}